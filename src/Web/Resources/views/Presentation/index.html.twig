{% extends 'Layout/base.html.twig' %}

{% block content %}
	{% for presentation in pagerfanta %}
		<div class="row">
			<p>{{ presentation.title }}</p>
			<p>{{ presentation.author }}</p>
			<p>{{ presentation.description }}</p>
			<video controls>
				<source src="{{ path('stream', {'path' : '/1e/fc/32571be34f3089a6fb78111ee035.mp4'}) }}" type="video/mp4">
				Your browser does not support HTML5 video.
			</video>
		</div>
	{% endfor %}
	{% if pagerfanta.hasNextPage() %}
		<li>
		    <a href="{{ path('homepage',{page: pagerfanta.getNextPage()}) }}">Następny</a>
		</li>
	{% endif %}
{% endblock %}
